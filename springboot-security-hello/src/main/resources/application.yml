server:
  port: 8080

logging:
  level:
    root: info
    org.springframework.web: INFO
    org.springframework.security: INFO

spring:
  thymeleaf:
    cache: false


  output:
    ansi:
      enabled: always